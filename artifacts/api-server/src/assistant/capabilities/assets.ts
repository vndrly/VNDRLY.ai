import { capabilityTools } from "./types";
export const ASSET_CAPABILITY_TOOLS = capabilityTools("asset_custody", "asset lookup, checkout, return, condition, and evidence", "events.subscribe", ["admin", "partner", "vendor", "field_employee"]);
