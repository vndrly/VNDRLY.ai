import { FormEvent, useState } from "react";
import { Bot, X } from "lucide-react";
import { PngPillButton } from "@/components/png-pill-rollover";

type Answer = { text: string; href: string; label: string };
export default function PublicAskV() { const [open, setOpen] = useState(false); const [message, setMessage] = useState(""); const [answer, setAnswer] = useState<Answer | null>(null); const [busy, setBusy] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { const response = await fetch("/api/public/askv", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ message, locale: document.documentElement.lang === "es" ? "es" : "en" }) }); const body = await response.json(); setAnswer(response.ok ? body : { text: body.message ?? "AskV is unavailable right now.", href: "mailto:support@vndrly.ai", label: "Contact Support" }); } catch { setAnswer({ text: "AskV is unavailable right now. You can still explore VNDRLY or contact Support.", href: "mailto:support@vndrly.ai", label: "Contact Support" }); } finally { setBusy(false); } }
  return <><button type="button" onClick={() => setOpen(true)} className="fixed bottom-5 right-5 z-30 flex min-h-12 items-center gap-2 rounded-full border bg-card px-5 font-bold shadow-lg outline-none focus-visible:ring-2 focus-visible:ring-[var(--brand-primary)]" aria-haspopup="dialog"><Bot className="h-5 w-5 text-[var(--brand-primary)]"/>AskV</button>{open && <section role="dialog" aria-modal="false" aria-labelledby="public-askv-title" className="fixed bottom-20 right-4 z-40 w-[calc(100vw-2rem)] max-w-sm rounded-2xl border bg-card p-5 shadow-2xl"><div className="flex items-center justify-between"><div><h2 id="public-askv-title" className="font-bold">AskV</h2><p className="text-xs text-muted-foreground">Public product help only</p></div><button onClick={() => setOpen(false)} aria-label="Close AskV" className="rounded p-2 focus-visible:ring-2"><X className="h-4 w-4"/></button></div><p className="mt-4 text-sm text-muted-foreground">Ask about VNDRLY modules, roles, signup, legal pages, demos, or support. AskV cannot access accounts or operational data.</p><form onSubmit={submit} className="mt-4"><label htmlFor="public-askv-question" className="text-sm font-semibold">Your question</label><textarea id="public-askv-question" maxLength={800} required value={message} onChange={(event) => setMessage(event.target.value)} className="mt-2 min-h-24 w-full rounded-lg border bg-background p-3 outline-none focus-visible:ring-2 focus-visible:ring-[var(--brand-primary)]"/><PngPillButton type="submit" disabled={busy || !message.trim()} className="mt-3">{busy ? "Checking…" : "AskV"}</PngPillButton></form>{answer && <div aria-live="polite" className="mt-4 rounded-lg bg-muted p-3 text-sm"><p>{answer.text}</p><a className="mt-2 inline-block font-bold text-[var(--brand-primary)] underline" href={answer.href}>{answer.label}</a></div>}</section>}</>;
}
