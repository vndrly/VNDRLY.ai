import { render, screen } from "@testing-library/react"; import { QueryClient, QueryClientProvider } from "@tanstack/react-query"; import { it, expect, vi } from "vitest"; import { Assets } from "./assets";
vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ assets: [{ id: "a1", name: "Radio kit", category: "communications", status: "checked_out", holderUserId: 7, currentLocation: "user:7", condition: "good", hold: null }], capabilities: { canCheckOutAsset: true } }), { status: 200 })));
it("renders canonical custody inventory and location", async () => { render(<QueryClientProvider client={new QueryClient()}><Assets /></QueryClientProvider>); expect(await screen.findByText("Radio kit")).toBeTruthy(); expect(screen.getByText(/user:7/)).toBeTruthy(); });
