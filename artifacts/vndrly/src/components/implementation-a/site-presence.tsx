import { ImplementationSurface } from "./surface";
export function SitePresence({ people = [], canSeeExactLocation = false }: { people?: Array<{ id: string; name: string; employer: string; site: string; exactLocation?: string }>; canSeeExactLocation?: boolean }) { return <ImplementationSurface module="sitePresence" title="Site Presence" description="Authorized on-duty and on-site visibility across connected work."><ul className="grid gap-3">{people.map(person => <li key={person.id} className="rounded-lg border p-4"><strong>{person.name}</strong><p className="text-sm">{person.employer} · {person.site}</p>{canSeeExactLocation && person.exactLocation && <p className="text-xs text-muted-foreground">{person.exactLocation}</p>}</li>)}</ul>{!people.length && <p className="text-sm text-muted-foreground">No authorized workers are currently visible.</p>}</ImplementationSurface>; }
