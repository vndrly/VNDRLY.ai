import CrewMapPage from "@/pages/crew-map";

/** Foreman portal crew map — vendor-scoped live GPS for field employees. */
export default function ForemanCrewMapPage() {
  return <CrewMapPage portalMode="foreman" />;
}
