/**
 * Field / foreman portal content pane — matches office dashboard layout
 * (`dashboard.tsx` uses `space-y-6` with no max-width cap inside main).
 * Shell `<main>` already applies `p-6`; pages should not add another cap.
 */
export const FIELD_OPS_PAGE_CLASS = "space-y-6 w-full";
