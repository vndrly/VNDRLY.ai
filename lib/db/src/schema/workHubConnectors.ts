import { index, integer, jsonb, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core"; import { usersTable } from "./users";
export const workHubCalendarConnectionsTable = pgTable("work_hub_calendar_connections", { id: uuid("id").primaryKey().defaultRandom(), ownerOrgType: text("owner_org_type").notNull(), ownerOrgId: integer("owner_org_id").notNull(), provider: text("provider").notNull(), capabilities: text("capabilities").array().notNull(), encryptedCredentials: text("encrypted_credentials").notNull(), status: text("status").notNull().default("connected"), createdById: integer("created_by_id").notNull().references(() => usersTable.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), revokedAt: timestamp("revoked_at", { withTimezone: true }) }, (t) => ({ ownerProviderUnique: uniqueIndex("work_hub_calendar_connection_unique").on(t.ownerOrgType, t.ownerOrgId, t.provider) }));
export const workHubExternalCalendarsTable = pgTable("work_hub_external_calendars", { id: uuid("id").primaryKey().defaultRandom(), connectionId: uuid("connection_id").notNull().references(() => workHubCalendarConnectionsTable.id, { onDelete: "cascade" }), externalId: text("external_id").notNull(), name: text("name").notNull(), selected: text("selected").notNull().default("false") }, (t) => ({ externalUnique: uniqueIndex("work_hub_external_calendar_unique").on(t.connectionId, t.externalId) }));
export const workHubExternalEventsTable = pgTable("work_hub_external_events", { id: uuid("id").primaryKey().defaultRandom(), calendarId: uuid("calendar_id").notNull().references(() => workHubExternalCalendarsTable.id, { onDelete: "cascade" }), externalId: text("external_id").notNull(), title: text("title").notNull(), startsAt: timestamp("starts_at", { withTimezone: true }).notNull(), endsAt: timestamp("ends_at", { withTimezone: true }).notNull(), location: text("location"), organizer: text("organizer"), attendeeDisplay: jsonb("attendee_display").$type<unknown[]>(), updateMarker: text("update_marker"), cancelledAt: timestamp("cancelled_at", { withTimezone: true }), syncedAt: timestamp("synced_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ externalUnique: uniqueIndex("work_hub_external_event_unique").on(t.calendarId, t.externalId), timeIdx: index("work_hub_external_event_time_idx").on(t.startsAt, t.endsAt) }));
export const workHubSyncCursorsTable = pgTable("work_hub_sync_cursors", { id: uuid("id").primaryKey().defaultRandom(), connectionId: uuid("connection_id").notNull().references(() => workHubCalendarConnectionsTable.id, { onDelete: "cascade" }), resourceType: text("resource_type").notNull(), cursor: text("cursor"), lastSuccessAt: timestamp("last_success_at", { withTimezone: true }), lastErrorCode: text("last_error_code"), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ resourceUnique: uniqueIndex("work_hub_sync_cursor_unique").on(t.connectionId, t.resourceType) }));
export const workHubSearchDocumentsTable = pgTable("work_hub_search_documents", { id: uuid("id").primaryKey().defaultRandom(), ownerOrgType: text("owner_org_type").notNull(), ownerOrgId: integer("owner_org_id").notNull(), contextKind: text("context_kind").notNull(), contextId: text("context_id").notNull(), subjectType: text("subject_type").notNull(), subjectId: text("subject_id").notNull(), visibilityRevision: text("visibility_revision").notNull(), title: text("title"), searchableText: text("searchable_text").notNull(), metadata: jsonb("metadata").$type<Record<string, unknown>>(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ subjectUnique: uniqueIndex("work_hub_search_document_unique").on(t.subjectType, t.subjectId), ownerIdx: index("work_hub_search_owner_idx").on(t.ownerOrgType, t.ownerOrgId, t.updatedAt) }));
