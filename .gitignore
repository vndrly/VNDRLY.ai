# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared
.expo-smoke-export

# dependencies
node_modules
.pnpm-store

# migration / scratch uploads (never commit)
.tmp_*
.tmp_combined*
tsconfig.zip
lib/db/drizzle/batches/*.json
.pnpm-store/

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings
.tmp_*
tsconfig.zip
*.err.log
*.out.log

# System Files
.DS_Store
Thumbs.db

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Environment (secrets)
.env
.env.local
.env.*.local

# Local scratch / automation output
.cache/
.local/
lib/e2e/scripts/.replit-profile/

# Per-run output of `pnpm --filter @workspace/api-server run eval`. The
# nightly workflow uploads this to the trend branch; locally it's just
# scratch.
eval-summary.json
vitest-results.json
.claude/
